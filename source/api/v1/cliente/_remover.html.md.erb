## <b class="tagdelete">DELETE</b> Remover o cadastro

<%
url = <<-Conteudo
 =>\n              '#{current_page.data.url}/cliente/'
            . $objParametro->url->cliente,
Conteudo

parametro_url = <<-Conteudo
        'cliente' => 'e3eb01524dd35282c9910a9473c34e8f',
Conteudo
%>
<%= partial "./api/v1/tema/codigo.html.md.erb", locals: {
    tipo: 'delete',
    descricao: 'Para remover o cadastro do cliente',
    url: url,
    parametro_url: parametro_url
} %>

<%= partial "./api/v1/tema/_retorno.html.md.erb", locals: {
    erro: false,
    servico: 'cliente',
    codigo: 200,
    mensagens: ['SUC075K01'],
    itens: []
} %>

<%= partial "./api/v1/tema/_retorno.html.md.erb", locals: {
    erro: true,
    servico: 'cliente',
    codigo: 400,
    mensagens: ['ERR07B401']
} %>

<code><%= current_page.data.url %>/cliente/<b>{cliente}</b></code>

Responsável por remover o cadastro de um cliente (pagador do boleto).
Para remover o cadastro de um cliente, é necessário fornecer o ID do cliente, que é um código único gerado pelo sistema ao cadastrar o cliente.

No entanto, é importante observar que não é possível remover um cadastro que já está vinculado a uma cobrança.
Isso significa que, se o cliente (pagador) já realizou algum pagamento ou tem alguma cobrança pendente, o seu cadastro não poderá ser removido.

<%= partial "./api/v1/tema/parametro_uri.html.md.erb", locals: {
    conteudo: <<-Conteudo
cliente | MD5 de identificação do cliente.
    Conteudo
} %>

<%= partial "./api/v1/tema/parametro_cabecalho.html.md.erb" %>

<%= partial "./api/v1/tema/ems.html.md.erb", locals: {
    mensagens: [
        'SUC075K01',

        'ERR04B801',

        'ERR07B401',

        'ERR03B501', 'ERR03B502', 'ERR03B503', 'ERR03B504', 'ERR03B505',
        'ERR03B506', 'ERR03B507',
    ]
} %>